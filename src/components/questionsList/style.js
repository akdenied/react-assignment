import {
  Divider,
  List,
  ListItem,
  ListItemText,
  Typography,
} from "@mui/material";
import styled from "styled-components";

export const StyledList = styled(List)`
  text-decoration: none;
  text-align: left;
`;
